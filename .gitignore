# Local Grafana state (we provision everything, so persisted state isn't tracked)
grafana-storage/
*.db

# Env overrides
.env
.env.local

# Local-only compose override (used to spin up the image renderer; carries a token)
docker-compose.override.yml
docker-compose.*.local.yml

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
